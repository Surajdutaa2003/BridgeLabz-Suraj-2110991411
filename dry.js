
function calculateDiscount(price) {
    return price * 0.1;
  }
  function calculateTax(price) {
    return price * 0.05;
  }
  
  // Use this:
  function calculate(price, rate) {
    return price * rate;
  }
  